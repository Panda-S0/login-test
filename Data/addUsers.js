//import the users array
import users from "./Users"

//adding a user to it using the addUser function
function addUser(usr) {
  users.push(usr)
}

//exporting the addUser funciton
export default addUser
